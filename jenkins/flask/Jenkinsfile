pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Pipeline is building'
                sh('docker image ls')
            }
        }
        stage('Push') {
            steps {
                echo 'Pipeline is pushing'
            }
        }
    }
}