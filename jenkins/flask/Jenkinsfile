pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Pipeline is building'
                docker image ls
            }
        }
        stage('Push') {
            steps {
                echo 'Pipeline is pushing'
            }
        }
    }
}